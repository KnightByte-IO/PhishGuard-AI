# Local development — uses Vite proxy to http://localhost:5000
VITE_API_URL=/api

# Production (npm run build) — use frontend/.env.production instead
# VITE_API_URL=https://phishguard-ai-api-qdkr.onrender.com/api
